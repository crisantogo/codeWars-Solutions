// Create a function 'numberToPower(number, power)' that "raises" the number up to power (ie multiplies number by itself power times). 

function numberToPower(number, power){
    // Code here
    return number**power
    //Alternatively;
    //return Math.pow(number, power)
}